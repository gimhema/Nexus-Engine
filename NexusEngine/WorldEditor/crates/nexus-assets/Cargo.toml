[package]
name = "nexus-assets"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
nexus-core.workspace   = true
nexus-render.workspace = true
png.workspace          = true
# 글자 래스터 (P7-E). egui 도 쓰는 크레이트라 의존성이 늘지 않는다.
ab_glyph.workspace     = true

[lints]
workspace = true
